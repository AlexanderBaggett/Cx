float fsum(const float *a, int n) { float s = 0; for (int i = 0; i < n; i++) s += a[i]; return s; }
int   isum(const int *a, int n)   { int s = 0;   for (int i = 0; i < n; i++) s += a[i]; return s; }
